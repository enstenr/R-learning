---
title: "Flood_analysis"
output:
  pdf_document: default
  html_document: default
date: "2025-10-04"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

```{r}
#insall once 
#install.packages("terra")

library(terra)

flood_raster <- rast(ncols=100, nrows=100)

set.seed(42)
values(flood_raster) <- rexp(ncell(flood_raster), rate = 0.02)

summary(values(flood_raster))

hist(values(flood_raster), main="Simulated Flood Raster", 
     xlab="Pixel Values", col="skyblue", breaks=25)

# Apply log transformation
log_raster <- log(values(flood_raster) + 1)  # +1 to avoid log(0)

# Histogram after log transform
hist(log_raster, main="Log-Transformed Flood Raster", 
     xlab="Log(Pixel Values + 1)", col="orange", breaks=50)


```
